## What does this change?

<!-- Short description. Link issues with "Fixes #123". -->

## Checklist

- [ ] `python build.py` and `python tools/check_site.py` pass
- [ ] Catalog changes: `python tools/validate_packages.py --elevation` reports 0 errors and icons were checked
- [ ] Engine or CLI changes: `python tools/test_script.py --run` passes on Windows; manual tests are described below
- [ ] `CHANGELOG.md` updated under "Unreleased"

## How was it tested?
